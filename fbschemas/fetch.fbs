namespace grocery.fb;

enum TaskType:byte { UNKNOWN = 0, FETCH = 1 }

table ItemQty {
  item:string;
  qty:double;
}

table FetchTask {
  task_id:string;
  task_type:TaskType;
  items:[ItemQty];
  timestamp_ms:long;
}

root_type FetchTask;
